#Ensure u r in reqd branch > git status; Tag is to be created as -
    #  > git tag v0.1.1, > git push origin v0.1.1
#Name of WorkFlow
name: Release
#Run on every commit tag which begins with "v" ( eg, "v0.1.4")
on:
  push:  
    tags: 
     - "v*"

#Build & create artifacts in GitHub 
jobs: 
#region 'JOB 1 : RELEASE'
  release: 
    name: "Release"
    runs-on: ubuntu-latest    

    steps:
    - name: Create release
      id: create_release
      uses: actions/create-release@v1
      env:
      # Token provided by GHActions, we don't need to create our own token
        GITHUB_TOKEN: ${{ secrets.github_token }} 
      with:
        tag_name: ${{ github.run_number }}  #pre-configured env var that is unique
        release_name: Release ${{ github.run_number }}
        body: New release for ${{ github.sha }}. Release notes LINK can be put here .
        draft: false
        prerelease: false        